---
apiVersion: v1
kind: List
items:
  - apiVersion: install.tetrate.io/v1alpha1
    #kind: IngressGateway
    kind: Gateway
    metadata:
      name: bookinfo-ugw
      namespace: bookinfo
    spec:
      type: UNIFIED
      #eastWestOnly: true
      kubeSpec:
        service:
          type: LoadBalancer
          #annotations:
          #  external-dns.alpha.kubernetes.io/hostname: gateway.tetrate.io